const submitButtonContact = document.getElementById('submitContact');
submitButtonContact.addEventListener('click', function(event) {
    submitButtonContact.preventDefault();
    alert('We will be in contact with you Shortly!');
});